/* This is the solution to FOURPTS */
#include <bits/stdc++.h>

using namespace std;

int pts[4][2];

int main(void){
    ios::sync_with_stdio(false);
    int t;
    cin >> t;

    while(t--){
        for(int i = 0; i < 4; i++){
            cin >> pts[i][0] >> pts[i][1];
        } 
    }
}