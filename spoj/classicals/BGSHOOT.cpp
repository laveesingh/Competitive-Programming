#include <iostream>
#include <algorithm>

using namespace std;

class moment{
	public:
		int time, animals;
		moment(int t, int a){
			time = t;
			animals = a;
		}
};

int main(void){
	int n, q, x, y, l, r;
	int no_ani[100002];
	fill_n(no_ani, 100000, 0);
	// scanf("%d", &n);
	// for (int i = 0;  i < n; i += 1){
	// 	scanf("%d %d", &x, &y);

	// }
	for (int i = 0; i < 1000000000; i += 1){
		q = 0;
		y = 0;
		r = 0;
	}
	cout << "Done" << endl;
}