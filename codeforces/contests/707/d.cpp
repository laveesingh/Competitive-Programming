#include <bits/stdc++.h>

using namespace std;

#define vi vector<int>

int main(void){
	ios::sync_with_stdio(false);
	int n, m, q;
	cin >> n >> m >> q;
	vector<pair<vi, int>> store(q+1, {vi()}); // stores the entire stage and the answer correspondingly

}