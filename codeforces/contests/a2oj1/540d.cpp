#include <bits/stdc++.h>

using namespace std;

const int N = 101;
double dp[N][N][N];
int main(void){
  ios::sync_with_stdio(false);
  cin.tie(NULL);
  int r, s, p;
  cin >> r >> s >> p;
  for(int i = r; i >= 0; i--)
    for(int j = s; j >= 0; j--)
      for(int k = p; k >= 0; k--){

}

